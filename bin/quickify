#!/bin/sh

if [ -z "$1" -o ! -f "$1.in" -o -z "$2" ]; then
    echo "Usage: $0 src/gwt/module.gwt.xml user_agent[,user_agent]"
    exit 255
fi

head -2 $1.in > $1
echo "  <set-property name=\"user.agent\" value=\"$2\"/>" >> $1
tail -n +3 $1.in >> $1
