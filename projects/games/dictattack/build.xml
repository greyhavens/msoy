<!-- build configuration -->
<project name="dictattack" default="compile" basedir=".">

  <!-- define some basic project parameters -->
  <property name="app.name"   value="DictionaryAttack"/>
  <property name="root.path"  value="../../.."/>
  <property name="flex.path"  value="${root.path}/projects/tools/extern/flex_sdk"/>
  <property file="build.properties"/>

  <!-- builds our Flash game -->
  <target name="compile">
    <java jar="${flex.path}/lib/mxmlc.jar" fork="true" failonerror="true">
      <arg value="-load-config"/>
      <arg value="${root.path}/etc/msoy-config.xml"/>
      <arg value="-compiler.source-path=."/>
      <arg value="-file-specs"/>
      <arg value="${app.name}.as"/>
    </java>
  </target>

  <!-- cleans out the compiled code -->
  <target name="clean">
    <delete>
      <fileset file="${app.name}.swf"/>
    </delete>
  </target>

  <!-- compiles and then jams the SWF into the document tree with the right filename -->
  <target name="dist" depends="compile">
    <fail unless="media_hash">Create a build.properties file with media_hash in it.</fail>
    <copy file="${app.name}.swf" tofile="${root.path}/pages/media/${media_hash}.swf"/>
  </target>

</project>
