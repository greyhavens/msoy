//
// $Id$

package com.threerings.msoy.data.all;

/**
 * Auto-generated build configuration class.
 */
public class DeploymentConfig
{
    /** A prefix used to access media proxied through the game server. */
    public static final String PROXY_PREFIX = "/remedia/";

    /** The time at which the client was built. */
    public static final String buildTime = "@build_time@";

    /** The version number assigned to this build. */
    public static final String version = "@build_version@";

    /** The URL from which we should make service requests. */
    public static final String serverURL = "@server_url@";

    /** The hostname of the server. */
    public static final String serverHost = "@server_host@";

    /** The URL via which we should load our media. */
    public static final String mediaURL = "@media_url@";

    /** The URL via which we should load our static media. */
    public static final String staticMediaURL = "@static_media_url@";

    /** Whether we're running on dev or in production. */
    public static final boolean devDeployment = @dev_deployment@;

    /** The public key for our RECAPTCHA service or "" if it's disabled. */
    public static final String recaptchaPublic = "@recaptcha_public@";
    
    /** The URL to reach billing. */
    public static final String billingURL = "@billing_url@";
    
    /** Our Facebook API key. */
    public static final String facebookKey = "@facebook_api_key@";
}
