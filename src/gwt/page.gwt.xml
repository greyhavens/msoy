<module>
  <inherits name="global"/>

  <!-- this doesn't exist, but if we don't have something GWT helpfully adds -->
  <!-- the whole goddamned source tree from the root on up -->
  <source path="client/page"/>

  <public path="public/page"/>
  <stylesheet src="/themed/css/page.css?themeId=0"/>
</module>
